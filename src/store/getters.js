const getters = {
    username(state) {
        return state.username;
    },
    cartCount(state) {
        return state.cartCount;
    },
    myAvatar(state) {
        return state.myAvatar;
    }
};

export default getters;
